      $(document).ready(function(){
      	// Navigation Init
    $("#navigation").navigation({
      mobileBreakpoint:"768",
       mobileBreakpoint:"1024",
    });

    // AOS Animation 
		AOS.init();

});